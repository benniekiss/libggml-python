name: "Renovate Schedule"

on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log Level'
        required: true
        default: 'info'
        type: choice
        options:
          - info
          - debug
          - warning
  schedule:
    - cron: '0 8 */1 * *'

env:
    LOG_LEVEL: ${{ inputs.logLevel }}

jobs:
  renovate:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: run renovate
        env:
          RENOVATE_REPOSITORIES: "[\"benniekiss/libggml-python\"]"
        uses: renovatebot/github-action@v43.0.2
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
